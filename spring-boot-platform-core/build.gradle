description = 'Spring Boot Platform Core'

dependencies {

    api project(':spring-boot-platform-core-api')

    api libs.jackson
    api libs.springBoot

    testImplementation libs.testbase
    testImplementation libs.springBootTest
}