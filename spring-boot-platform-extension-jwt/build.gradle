description = 'Spring Boot Platform JWT'

dependencies {
    api libs.platformApi

    api project(':spring-boot-platform-api')
    api project(':spring-boot-platform-core')
    implementation libs.jwt

    testImplementation libs.platformTest
}